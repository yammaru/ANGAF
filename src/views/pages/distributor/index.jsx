import React, { Fragment } from "react";
import DistribuitorSection from "../../components/DistribuitorSection/DistribuitorSection";


const DistribuitorPage = () => {
	return (
		<Fragment>
			<DistribuitorSection />
		</Fragment>
	);
};

export default DistribuitorPage;
